rootProject.name = 'Recaf'

include 'recaf-core'
include 'recaf-ui'

include ':libs:vineflower'
// project(':vineflower').projectDir = new File(settingsDir, 'libs/vineflower')

buildCache {
    local {
        enabled = true
        directory = new File('build/cache')
    }
}
